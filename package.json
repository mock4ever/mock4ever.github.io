{
  "name": "mock4ever-blog",
  "private": true,
  "version": "0.0.2",
  "scripts": {
    "deploy": "yarn build && gatsby build && gh-pages -d public -b master",
    "start": " gatsby develop",
    "build": "yarn clean && gatsby build",
    "clean": "gatsby clean"
  },
  "dependencies": {
    "gatsby": "^2.20.12",
    "gatsby-theme-blog": "^1.4.12",
    "react": "^16.12.0",
    "react-dom": "^16.12.0"
  },
  "devDependencies": {
    "gh-pages": "^2.2.0"
  }
}
